brew install readline -dev

IMPORTANT: Bash Reference Manual
https://www.gnu.org/software/bash/manual/bash.html#Shell-Operation

ERROR CODES: 
0 = successful
1 = general error (command error, etc.)
2 = syntax error (parsing)
126 = (...) is a directory
127 = command not found (after parsing) or CTRL+D
130 = CTRL+C
131 = CTRL+\

On malloc error, exit program.

builtins
exit: adds or subtracts [int] from exit code

plan for parsing: start with $ exceptions, first count the amount of characters to subtracts and store 
the argument in a char *, then check if the env variable exists. if yes, add to char count, otherwise do nothing

POUR SEMAINE DE LA PISCINE::::
Implémenter le check_env(double check it pls) avec le compte de mini->parse.sub
Implémenter les substitutions
S'assurer que les quote_cmd sont bien écrit avec les bonnes variables

Pour jeudi:
S'assurer que les blocs du milieu ont pas de fautes.